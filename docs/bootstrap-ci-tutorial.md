# Bootstrap CI Tutorial

Call `krippendorff_alpha(data, scale="ordinal")` to receive `(value, ci_low, ci_high)`. Wide intervals mean the overlap set is too small or too noisy for a stable reviewer-quality decision.
